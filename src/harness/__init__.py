"""Experiment harness: everything that runs on the cluster.

Standard library only — these modules must run with the cluster python3,
without uv or any third-party package. Analysis code lives in src/analysis.
"""
